# Server port
PORT=3001

# Frontend URL (used for CORS)
CLIENT_URL=https://your-domain.com

# Session secret (not currently used by the app, reserved for future use)
SESSION_SECRET=change_me_in_production

# Node environment
NODE_ENV=development
